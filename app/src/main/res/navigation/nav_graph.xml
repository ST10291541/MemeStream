<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/feedFragment">

    <fragment
        android:id="@+id/feedFragment"
        android:name="vcmsa.projects.memestreamapp.FeedFragment"
        android:label="Feed"
        tools:layout="@layout/fragment_feed" />

    <fragment
        android:id="@+id/createMemeFragment"
        android:name="vcmsa.projects.memestreamapp.MemeFragment"
        android:label="Create Meme"
        tools:layout="@layout/fragment_meme" />

    <fragment
        android:id="@+id/mapFragment"
        android:name="vcmsa.projects.memestreamapp.MapFragment"
        android:label="Map"
        tools:layout="@layout/fragment_map" />

    <fragment
        android:id="@+id/profileFragment"
        android:name="vcmsa.projects.memestreamapp.ProfileFragment"
        android:label="Profile"
        tools:layout="@layout/fragment_profile" />

</navigation>
